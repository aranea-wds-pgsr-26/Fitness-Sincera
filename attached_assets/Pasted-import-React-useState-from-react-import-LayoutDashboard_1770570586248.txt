import React, { useState } from 'react';
import { 
  LayoutDashboard, 
  Home, 
  Apple, 
  BarChart3, 
  Users, 
  MessageSquare, 
  Search, 
  Bell, 
  ChevronDown, 
  Zap, 
  Heart, 
  Activity, 
  Percent, 
  Moon,
  MoreVertical,
  Rocket
} from 'lucide-react';

/**
 * Componente principal do Painel de Saúde Flux
 * Recria o design moderno com Tailwind CSS e Lucide Icons
 */
const App = () => {
  const [activeTab, setActiveTab] = useState('Dashboard');

  return (
    <div className="flex h-screen bg-[#111111] font-sans text-slate-900 overflow-hidden">
      {/* Barra Lateral (Sidebar) */}
      <aside className="w-64 bg-[#1a1c1e] flex flex-col p-6 text-slate-400">
        <div className="flex items-center gap-2 mb-10 pl-2">
          <div className="w-8 h-8 bg-[#d4f54c] rounded-lg flex items-center justify-center">
            <Zap className="w-5 h-5 text-black fill-black" />
          </div>
          <span className="text-2xl font-bold text-white tracking-tight">flux</span>
        </div>

        <nav className="flex-1 space-y-2">
          <NavItem 
            icon={<LayoutDashboard size={20} />} 
            label="Dashboard" 
            active={activeTab === 'Dashboard'} 
            onClick={() => setActiveTab('Dashboard')}
            badge={3}
          />
          <NavItem 
            icon={<Home size={20} />} 
            label="Início" 
            active={activeTab === 'Home'} 
            onClick={() => setActiveTab('Home')}
          />
          <NavItem 
            icon={<Apple size={20} />} 
            label="Nutrição" 
            active={activeTab === 'Nutrition'} 
            onClick={() => setActiveTab('Nutrition')}
          />
          <NavItem 
            icon={<BarChart3 size={20} />} 
            label="Relatórios" 
            active={activeTab === 'Reports'} 
            onClick={() => setActiveTab('Reports')}
            badge={1}
          />
          <NavItem 
            icon={<Users size={20} />} 
            label="Utilizadores" 
            active={activeTab === 'Users'} 
            onClick={() => setActiveTab('Users')}
          />
          <NavItem 
            icon={<MessageSquare size={20} />} 
            label="Mensagens" 
            active={activeTab === 'Messages'} 
            onClick={() => setActiveTab('Messages')}
          />
        </nav>

        {/* Card de Upgrade */}
        <div className="mt-auto bg-[#d4f54c] rounded-3xl p-5 relative overflow-hidden group cursor-pointer">
          <div className="relative z-10">
            <h4 className="text-black font-bold text-lg mb-1">Upgrade para Pro</h4>
            <p className="text-black/70 text-xs mb-4">Atualize a sua conta para uma experiência completa.</p>
            <button className="w-full bg-black text-white py-3 rounded-2xl text-sm font-semibold hover:bg-zinc-800 transition-colors">
              Upgrade Agora
            </button>
          </div>
          <Rocket className="absolute -right-2 -bottom-2 w-20 h-20 text-black/10 -rotate-12 group-hover:scale-110 transition-transform" />
        </div>
      </aside>

      {/* Conteúdo Principal */}
      <main className="flex-1 bg-[#e9e9e9] rounded-l-[40px] overflow-y-auto p-8">
        {/* Cabeçalho */}
        <header className="flex items-center justify-between mb-8">
          <div className="flex items-center gap-3">
            <img 
              src="https://images.unsplash.com/photo-1500648767791-00dcc994a43e?w=100&h=100&fit=crop" 
              alt="Perfil" 
              className="w-10 h-10 rounded-full border-2 border-white shadow-sm"
            />
            <div>
              <div className="flex items-center gap-1 cursor-pointer">
                <span className="font-semibold text-sm">Lucas Bennett</span>
                <ChevronDown size={14} />
              </div>
              <p className="text-[10px] text-slate-500">bennet02@gmail.com</p>
            </div>
          </div>

          <div className="flex items-center gap-4">
            <div className="relative">
              <Search className="absolute left-3 top-1/2 -translate-y-1/2 text-slate-400" size={18} />
              <input 
                type="text" 
                placeholder="Pesquisar..." 
                className="bg-white rounded-full py-2 pl-10 pr-4 text-sm w-64 focus:outline-none focus:ring-2 focus:ring-[#d4f54c]/50"
              />
            </div>
            <div className="relative p-2 bg-white rounded-full shadow-sm cursor-pointer">
              <Bell size={18} />
              <span className="absolute top-1 right-1 w-4 h-4 bg-[#d4f54c] border-2 border-white rounded-full text-[8px] flex items-center justify-center font-bold">2</span>
            </div>
          </div>
        </header>

        {/* Título da Secção */}
        <section className="mb-8 flex justify-between items-end">
          <div>
            <h1 className="text-4xl font-bold tracking-tight mb-1">Visão Geral de Saúde</h1>
            <p className="text-slate-500 text-sm">Tome o controlo da sua saúde hoje!</p>
          </div>
          <div className="flex flex-col items-end gap-2">
            <span className="text-xs text-slate-400 font-medium uppercase tracking-wider">12 Julho, 2024</span>
            <button className="bg-white px-4 py-2 rounded-xl text-sm font-semibold flex items-center gap-2 shadow-sm border border-slate-100">
              Hoje <ChevronDown size={14} />
            </button>
          </div>
        </section>

        {/* Grelha de Cartões (Grid) */}
        <div className="grid grid-cols-12 gap-6">
          
          {/* Cartão Energia Usada */}
          <div className="col-span-4 bg-white rounded-[32px] p-6 shadow-sm flex flex-col h-full min-h-[450px]">
            <div className="flex justify-between items-start mb-6">
              <div className="flex items-center gap-2">
                <div className="p-2 bg-slate-50 rounded-lg"><Zap size={18} className="text-slate-400" /></div>
                <span className="font-bold text-slate-700">Energia Usada</span>
              </div>
              <button className="text-slate-300 hover:text-slate-600"><MoreVertical size={20} /></button>
            </div>

            <div className="mb-8">
              <div className="flex items-baseline gap-1">
                <span className="text-4xl font-bold">4,3k</span>
                <span className="text-slate-400 text-sm font-medium">kcal hoje</span>
                <span className="ml-2 bg-[#d4f54c] text-[10px] font-bold px-2 py-0.5 rounded-full">+5%</span>
              </div>
            </div>

            {/* Gráfico de Círculos Sobrepostos */}
            <div className="relative flex justify-center items-center h-48 mb-8">
              <div className="absolute w-32 h-32 rounded-full bg-[#b8b0f5] flex flex-col items-center justify-center -translate-x-10 translate-y-2 shadow-lg">
                <span className="text-xl font-bold text-black/80">2,6k</span>
                <span className="text-[10px] text-black/50">kcal</span>
              </div>
              <div className="absolute w-24 h-24 rounded-full bg-[#2a2d30] flex flex-col items-center justify-center translate-x-12 -translate-y-4 shadow-xl z-10">
                <span className="text-lg font-bold text-white">1,2k</span>
                <span className="text-[10px] text-white/50">kcal</span>
              </div>
              <div className="absolute w-16 h-16 rounded-full bg-[#d4f54c] flex flex-col items-center justify-center translate-x-4 translate-y-16 shadow-lg z-20">
                <span className="text-sm font-bold">500</span>
                <span className="text-[10px] text-black/60">kcal</span>
              </div>
            </div>

            {/* Barras de Atividade */}
            <div className="space-y-4 mt-auto">
              <ActivityRow label="Corrida" percent={45} color="bg-[#b8b0f5]" dotColor="bg-purple-400" />
              <ActivityRow label="Treinos" percent={30} color="bg-[#2a2d30]" dotColor="bg-slate-900" />
              <ActivityRow label="Caminhada" percent={25} color="bg-[#d4f54c]" dotColor="bg-[#d4f54c]" />
            </div>
          </div>

          {/* Coluna Central: Batimento e Atividade */}
          <div className="col-span-3 flex flex-col gap-6">
            <MetricCard 
              icon={<Heart size={18} className="text-slate-400" />}
              label="Batimento Cardíaco"
              value="62"
              unit="bpm"
              subText="Méd. 78 Bpm"
            />
            <MetricCard 
              icon={<Activity size={18} className="text-slate-400" />}
              label="Atividade"
              value="5,8"
              unit="km"
              subText="Ativo 75 Min"
            />
          </div>

          {/* Cartão Índice de Bem-estar */}
          <div className="col-span-5 bg-white rounded-[32px] p-6 shadow-sm flex flex-col">
            <div className="flex justify-between items-start mb-4">
              <div className="flex items-center gap-2">
                <div className="p-2 bg-slate-50 rounded-lg"><Percent size={18} className="text-slate-400" /></div>
                <span className="font-bold text-slate-700">Índice de Bem-estar</span>
              </div>
              <button className="text-slate-300 hover:text-slate-600"><MoreVertical size={20} /></button>
            </div>
            
            <div className="flex items-baseline gap-1 mb-6">
              <span className="text-4xl font-bold">78</span>
              <span className="text-slate-400 text-2xl font-medium">%</span>
              <span className="ml-2 bg-[#d4f54c] text-[10px] font-bold px-2 py-0.5 rounded-full">+10%</span>
            </div>

            {/* Gráfico de Pontos */}
            <div className="flex-1 grid grid-cols-12 gap-2 content-center">
              {[...Array(48)].map((_, i) => (
                <div 
                  key={i} 
                  className={`w-3 h-3 rounded-full ${
                    i > 20 ? 'bg-indigo-100' : 
                    i > 10 ? 'bg-indigo-300' : 'bg-indigo-500'
                  }`}
                  style={{ opacity: 0.3 + Math.random() * 0.7 }}
                ></div>
              ))}
            </div>
          </div>

          {/* Cartão Análise do Sono (Escuro) */}
          <div className="col-span-8 bg-[#1a1c1e] rounded-[32px] p-8 shadow-xl text-white">
            <div className="flex justify-between items-center mb-10">
              <div className="flex items-center gap-3">
                <div className="p-2 bg-zinc-800 rounded-xl"><Moon size={20} className="text-slate-300" /></div>
                <span className="font-bold text-lg">Análise do Sono</span>
              </div>
              <button className="bg-zinc-800 px-4 py-2 rounded-xl text-sm flex items-center gap-2 text-slate-300">
                Mensal <ChevronDown size={14} />
              </button>
            </div>

            <div className="grid grid-cols-2 gap-8 mb-10">
              <div className="flex items-center gap-4">
                <div className="h-12 w-1.5 bg-[#d4f54c] rounded-full"></div>
                <div>
                  <div className="flex items-baseline gap-1">
                    <span className="text-3xl font-bold">85</span>
                    <span className="text-xs text-slate-500">%</span>
                  </div>
                  <p className="text-xs text-slate-500 uppercase tracking-wider font-semibold">Eficiência do Sono</p>
                </div>
              </div>
              <div className="flex items-center gap-4">
                <div className="h-12 w-1.5 bg-[#b8b0f5] rounded-full"></div>
                <div>
                  <div className="flex items-baseline gap-1">
                    <span className="text-3xl font-bold">7h 15m</span>
                  </div>
                  <p className="text-xs text-slate-500 uppercase tracking-wider font-semibold">Duração do Sono</p>
                </div>
              </div>
            </div>

            {/* Gráfico de Barras do Sono */}
            <div className="flex items-end justify-between gap-2 h-40">
              <SleepBar month="Jun" height="h-24" />
              <SleepBar month="Jul" height="h-32" />
              <SleepBar month="Ago" height="h-28" />
              <SleepBar month="Set" height="h-44" active={true} />
              <SleepBar month="Out" height="h-20" />
              <SleepBar month="Nov" height="h-32" />
              <SleepBar month="Dez" height="h-28" />
            </div>
          </div>
        </div>
      </main>
    </div>
  );
};

// --- Subcomponentes auxiliares ---

const NavItem = ({ icon, label, active, onClick, badge }) => (
  <button 
    onClick={onClick}
    className={`w-full flex items-center justify-between p-3 rounded-2xl transition-all ${
      active ? 'bg-white text-black font-bold' : 'hover:bg-zinc-800/50 text-slate-400'
    }`}
  >
    <div className="flex items-center gap-3">
      <span className={active ? 'text-black' : 'text-slate-500'}>{icon}</span>
      <span className="text-sm">{label}</span>
    </div>
    {badge && (
      <span className={`text-[10px] w-5 h-5 flex items-center justify-center rounded-full font-bold ${
        active ? 'bg-[#d4f54c] text-black' : 'bg-zinc-700 text-slate-300'
      }`}>
        {badge}
      </span>
    )}
  </button>
);

const ActivityRow = ({ label, percent, color, dotColor }) => (
  <div className="space-y-1.5">
    <div className="flex justify-between items-center text-[11px] font-bold">
      <div className="flex items-center gap-2 uppercase tracking-wide">
        <span className="text-slate-800">{label}</span>
        <div className={`w-1.5 h-1.5 rounded-full ${dotColor}`}></div>
      </div>
      <span className="text-lg font-bold">{percent}<span className="text-[10px] ml-0.5 text-slate-400">%</span></span>
    </div>
    <div className="h-2 w-full bg-slate-100 rounded-full overflow-hidden">
      <div className={`h-full ${color}`} style={{ width: `${percent}%` }}></div>
    </div>
  </div>
);

const MetricCard = ({ icon, label, value, unit, subText }) => (
  <div className="bg-white rounded-[32px] p-6 shadow-sm flex flex-col flex-1">
    <div className="flex justify-between items-start mb-4">
      <div className="flex items-center gap-2">
        <div className="p-2 bg-slate-50 rounded-lg">{icon}</div>
        <span className="font-bold text-slate-700 text-xs">{label}</span>
      </div>
      <button className="text-slate-300"><MoreVertical size={16} /></button>
    </div>
    <div className="flex-1 flex flex-col justify-center">
      <div className="flex items-baseline gap-1">
        <span className="text-3xl font-bold">{value}</span>
        <span className="text-xs text-slate-400 font-medium">{unit}</span>
      </div>
    </div>
    <div className="mt-4 pt-4 border-t border-slate-50 flex justify-end">
      <span className="text-[10px] text-slate-400 font-bold uppercase tracking-wider">{subText}</span>
    </div>
  </div>
);

const SleepBar = ({ month, height, active }) => (
  <div className="flex flex-col items-center gap-4 flex-1">
    <div className="w-full flex gap-1 items-end justify-center h-full">
      <div className={`w-4 ${height} rounded-full ${active ? 'bg-[#d4f54c]' : 'bg-zinc-800 opacity-60'} border border-white/5`}></div>
      <div className={`w-4 ${height} rounded-full ${active ? 'bg-[#b8b0f5]' : 'bg-zinc-800 opacity-60'} border border-white/5`} style={{ opacity: 0.6 }}></div>
    </div>
    <span className={`text-[10px] font-bold uppercase ${active ? 'text-white' : 'text-slate-600'}`}>
      {month} {active && '↗'}
    </span>
  </div>
);

export default App;