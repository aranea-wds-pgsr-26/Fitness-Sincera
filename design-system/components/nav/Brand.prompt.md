The brand lock-up: lime Zap tile + wordmark, built for the dark sidebar / auth screens.

```jsx
<Brand variant="wordmark" />
<Brand variant="full" size={24} />
```

`variant`: `wordmark` (lowercase "flux") or `full` ("Fitness Sincera"). `onDark` controls text colour.
