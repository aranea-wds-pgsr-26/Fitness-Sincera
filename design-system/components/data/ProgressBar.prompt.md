Slim rounded progress track; the fill colour encodes the metric (lime energy, purple activity, green hydration).

```jsx
<ProgressBar value={78} tone="lime" />
<ProgressBar value={60} tone="green" height={6} />
```

`tone`: `lime, purple, green, slate`. Animates the fill width by default.
