Circular wellness gauge - thick rounded stroke arc with a big centred value. Purple by default.

```jsx
<RingGauge value={72} label="Wellness" />
<RingGauge value={68} valueText="68%" label="Completo" tone="lime" size={180} />
```

`tone`: `purple, lime, green`. Tune `size` / `stroke`.
