The white work surface every screen is built from - soft `--shadow-card`, generous radius, hairline border.

```jsx
<Card>conteúdo</Card>
<Card interactive radius="var(--radius-3xl)">conteúdo</Card>
```

`pad` toggles the default 24px padding; `interactive` enables the lift-on-hover used on clickable stat cards.
