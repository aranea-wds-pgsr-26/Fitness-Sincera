Initials or image avatar with an optional white ring (for stacking on coloured surfaces).

```jsx
<Avatar src="/u.jpg" initials="LB" />
<Avatar initials="CS" tone="dark" size={40} />
```

Tones: `slate, dark, green, lime`. Falls back to `initials` when `src` is absent.
